8.2 Ecrire le programme qui simule l’affichage d’un compteur digital à 3 chiffres
000, 001, …, 009, 010, …, 099, 100, …, 999

debut declaration compteur
    debut declaration variable
        const TAILLE:N <- 9
        var unite:N <- 0
        var dizaine:N <- 0
        var centaine:N <- 0
        var nbreTest:B <- false


    fin declaration variable
tant que nbreTest = false
    tant que unite<= TAILLE
        unite <- unite + 1
        afficher unite
        afficher dizaine
        afficher centaine
    fin tantque 
    si unite = 10
        alors unite <- 0
        dizaine <- dizaine +1
    finsi
    si dizaine = 10
        alors dizaine <- 0
        centaine <- centaine +1
    finsi
    si unite = TAILLE et dizaine = TAILLE et centaine = TAILLE
        alors 
        nbreTest <- true
    finsi
fin tant que 

fin compteur