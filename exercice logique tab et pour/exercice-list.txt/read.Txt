structure List
    valeur :N 
    suivant:List
fin List

fonction lire (in var l:List, in var position:N):N 
    var position <- l
    var valeur <- l.position

        tant que position != null
            valeur <- l.position
            position <- position.suivant
            afficher valeur
        fin tantque

    retourne valeur
fin lire