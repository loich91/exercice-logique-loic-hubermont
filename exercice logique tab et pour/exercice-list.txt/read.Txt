structure List
    valeur :N 
    suivant:List
fin List

fonction lire (in var l:List, in var position:N):N 
    var positionactuelle:N <- l
    var valeur :N <- 0
    var celtemp :list <- l

    pour positionactuelle allant de 1 à position
        valeur <- celtemp.valeur
        afficher valeur
        celtemp <- celtemp.suivant

    positionactuelle suivant
        fin tantque
        valeur <- celtemp.valeur

    retourne valeur
fin lire